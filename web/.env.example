# API base for the admin endpoints.
# Leave as the relative default for both dev (Vite proxy) and production
# (Django serves the SPA same-origin). Override only if the API lives elsewhere.
VITE_API_BASE=/api/admin
